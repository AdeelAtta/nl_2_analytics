apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: openquery
  labels:
    app: backend
    component: api
spec:
  type: ClusterIP
  ports:
    - port: 8100
      targetPort: 8100
      protocol: TCP
      name: http
  selector:
    app: backend
